# AI Build Log
- AI assisted with boilerplate setup for Express server.
- AI generated the DB schema and queries using standard `pg` logic.
- AI wrote the CORS middleware usage and `express-rate-limit` setup.
- Used AI to scaffold the Jest tests for endpoints.
